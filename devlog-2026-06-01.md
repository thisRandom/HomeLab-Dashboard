# 开发日志 — 2026-06-01

## 今日概览

今天完成了 **iKuai 设备控制功能**的开发，同时对实时网速大卡片进行了重构，并修复了主题切换相关 bug。

---

## 一、设备控制功能（新）

### 后端

- **新增 `DeviceController.java`**（未提交）
  - `GET /api/devices/traffic-ranking` — 设备流量排行（支持 topN 参数，默认10）
  - `GET /api/devices` — 获取局域网设备详情列表
  - `POST /api/devices/reboot` — 远程重启 iKuai 路由
  - `POST /api/devices/factory-reset` — 恢复出厂设置（危险操作，需二次确认）
  - 返回标准 `ApiResponse<T>` 结构

### 前端

- **新增 `device.ts`**（未提交）— 设备 API 封装
- **新增全屏确认弹窗组件** — 设备重启/恢复出厂的二次确认 UI

---

## 二、实时网速大卡片重构

| 文件 | 改动 |
|------|------|
| `RealtimeSpeedCard.vue` | +215 行大幅重构，优化图表渲染和数据显示 |
| `SystemOverviewCard.vue` | 修复主题切换时样式异常 |
| `SystemOverviewCard.vue` | 修复图表数据显示 bug |

---

## 三、数据库变更

| 文件 | 改动 |
|------|------|
| `schema.sql` | 新增 `card_metric_mapping` 表（卡片-指标映射） |
| `ikuai.sql` | 新增 iKuai 相关初始化数据 |
| `CardMetricMappingMapper.java` | 新增 MyBatis Mapper |

---

## 四、Dashboard 布局优化

- `DashboardView.vue` — 新增卡片类型支持（+49 行）
- `DashboardController.java` — 接口调整（+24 行）
- `MetricsController.java` — 指标接口增强（+122 行）
- 新增 `DeviceTrafficCard.vue` — 设备流量卡片组件（+181 行）

---

## 五、Git 提交记录

```
6a93c4e feat(dashboard): 实时网速大卡片重构
f1c2849 fix: 修复实时网速卡片图表/系统信息显示异常及主题切换问题
865b17f feat(frontend): 更新仪表盘及配置文件
4412896 feat: 添加 iKuai 设备控制功能（重启/恢复出厂）及全屏弹窗组件
233e775 feat: 更新设备控制功能及 API 配置
```

### ⚠️ 待提交文件

```
backend/src/main/java/.../controller/DeviceController.java  (933 bytes, 新增)
frontend/src/api/device.ts                                   (323 bytes, 新增)
```

---

## 六、关联事项

- **B站视频文案** 已完成 → [[bilibili-video-script.md]]
  - 含完整口播稿（带海螺配音停顿标记 `<#X#>`）
  - 配音风格推荐：中文男声 · 青年 · 自然叙述型

---

## 七、明日计划

- [ ] 提交未跟踪的设备控制文件（`DeviceController.java`、`device.ts`）
- [ ] 设备控制功能联调测试
- [ ] 视频录制准备（大屏效果录屏）
